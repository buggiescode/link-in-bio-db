<div>

  <% @list_of_items.each do |item| %>
    <div class="item"> 
      <img src="<%= item.thumbnail_url %>" class="thumbnail">
      
      <a class="link-text" target="_blank" href="<%= item.link_url %>">
        <%= item.link_description  %>
      </a>
    </div>
  <% end %>
</div>
